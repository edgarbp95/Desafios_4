let base = prompt ("Ingrese la base del triángulo: ");
let altura = prompt ("Ingrese la altura del triángulo: ");
let formulaArea = (base*altura)/2;
alert ("El area del triángulo es: " + formulaArea );